{
  "name": "mulundstays",
  "version": "1.0.0",
  "description": "MulundStays — Airbnb-style short-stay platform for Mulund, Mumbai",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:admin": "concurrently \"npm run dev:backend\" \"npm run dev:admin:frontend\"",
    "dev:admin:frontend": "cd frontend && npm run dev:admin",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:admin:frontend\"",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "build:admin": "cd frontend && npm run build:admin",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
