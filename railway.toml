# railway.toml — Deploy backend on Railway
[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[healthcheck]
path = "/api/health"
timeout = 10
